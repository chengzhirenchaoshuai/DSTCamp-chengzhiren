__pycache__/
*.pyc
*.pyo

# PyInstaller / packaging artifacts
build/
dist/
*.egg-info/
*.spec

# 开发/打包缓存集中目录（字节码缓存 __pycache__ + PyInstaller 中间产物 build），
# 放项目内方便随时整删，见 dstools/__init__.py 与 scripts/build_exe.py。
reference/_cache/

# 游戏脚本参考（约 277MB / 4000+ 文件，仅本地核对世界设置取值用，不进仓库）
reference/scripts-260814/

# 源码直跑时的运行时缓存（mod 图标/角色头像/mod 解析结果等），
# 正常应落在 %APPDATA%/DSTCamp/cache/，这里只防误入仓库。
cache/

# Agent-specific local instructions and Codex runtime state
AGENTS.md
CLAUDE.md
.codex/
